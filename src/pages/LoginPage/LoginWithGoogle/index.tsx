// import { logIn } from "features/Auth";

export const LoginWithGoogle = () => {
  return (
    <div>
      {/*<button onClick={() => logIn(setName, setToken)}>*/}
      Log in with google
      {/*</button>*/}
    </div>
  );
};
